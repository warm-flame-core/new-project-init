# 验证走查：模板生成完整性（testing/）

> 用途：**skill 迭代验证**（模板改完跑一遍「生成示例」验证结构完整）。每次改动模板后按受影响模板走查。
> 走查方式：用该模板生成一份示例产出（结构脱敏），对照「第一部分章节递归」逐节核对完整性。

## 前置

- [ ] 确认本次迭代改动的模板清单
- [ ] 读取对应模板全文（第一部分结构一览 + 第二部分详细规格）

## 走查步骤

1. **结构完整性**
   - [ ] 生成的示例覆盖第一部分「结构一览」的全部章节
   - [ ] 无章节缺失/错位/重复
   - [ ] 维护声明在头部、变更记录在末尾

2. **详细规格落实**
   - [ ] 第二部分每个最小标题的「写什么/格式/示例」都被使用
   - [ ] 无占位符残留（待定/TODO/类似任务 N 等，specs 类模板重点查）
   - [ ] 示例为结构示范，无 PTB-IMP 特有内容照抄（脱敏）

3. **on/off 与条件句**
   - [ ] 适用条件不满足的章节能跳过
   - [ ] 条件句（如 B4 多分支时、U13 要 CI 时）标注清楚

3b. **产出物门禁核对（v10.11，全量体检 ISSUE-002）**
   - [ ] 本模板对应产出物的「新建强制步骤」已与 SKILL.md「产出物新建门禁总览」一致
   - [ ] 产出新文件时能覆盖模板全部必核结构（含头部声明/维护声明/变更记录）
   - [ ] 存量完善补建文档时，产出物不会出现「把模板挪过去就不管」

4. **引用联动（改完必查）**
   - [ ] 模板内引用（如「见 CLAUDE.md C 区」「见模板 24」）目标存在
   - [ ] 被改动模板的「附录 C 索引 / 附录 B 骨架」描述与模板实际一致
   - [ ] 相关模板之间的互引（如 25↔24↔17）无断链

5. **变更记录**
   - [ ] 模板末尾「变更记录」表已追加本次迭代行（日期/内容/署名）

6. **可装配规范模块（v11.0，模板 27~31）**
   - [ ] 27~31 头部含「🧩 装配方式」标注（可选规范模块：按量裁决 / 默认关），与 SKILL.md「规范装配问询」表一致
   - [ ] 独立产出的规范文件登记进 DOCUMENT-INDEX + `<入口规范文件>` 信息闭环图（唯一出处）
   - [ ] 30/31 默认「关」：用户答「要」才产出；31 写文件类须配 30 worktree 隔离（互链无断链）

7. **署名与占位（v11.0，ISSUE-008/013）**
   - [ ] 模板署名措辞均为四段式 `<实体人>-<平台>-<角色>@<分支>`，无残留三元组 `<人>-<角色>-<实例>`
   - [ ] 产出物名/入口引用用 `<入口规范文件>` 占位，未硬编码 CLAUDE.md
   - [ ] 示例行/变更记录署名格式与本模板头部「维护声明」一致

## 走查记录

| 日期 | 走查人 | 模板 | 结果 | 发现的问题 |
|------|--------|------|------|------------|
| 2026-08-17 | Reasonix（skill 迭代） | 模板 v11.0 | ✅ 通过 | 核对 27~31 可装配标注 + 四段式署名 + `<入口规范文件>` 占位；模板 30/31 引用模板 24 阶段6 与模板 28 §5 合并规范均存在、无断链；产出物门禁表已含 30/31 |
| 2026-08-17 | Reasonix（skill 迭代） | 强门禁 RED-GREEN 实测 | ✅ 通过（真子代理执行） | **RED（无强门禁基线，真 subagent）**：题库外置 references（存量 92 行含 R1-6/4 轮/阶段0 A/B/C），SKILL.md 骨架仅标题级概览、无任何具体题目 → agent 是否主动读 references = 不确定，**不能保证仅凭骨架正确执行问询**；印证强门禁必要。**GREEN（有强门禁，真 subagent）**：跨平台强门禁硬规则1/2 明文强制先读全文、借口表第 3 行精确反驳 auto-fold 绕行、红线封死全部绕行路径 → 判定**有效**（规则层面保证，非沙箱级物理强制）。两子代理均纯只读（git status 无新增改动）。 |
| 2026-08-17 | Reasonix（skill 迭代） | 子代理上下文继承范围（v11.0） | ✅ 通过 | 已把「子代理上下文继承档位」纳入文档：DSH/Reasonix adaptation 各加 2b 档位表（后台派发不继承 / 继承 / workflow / 常驻），配「异常提示」（流程被精简/题目缺失→上报父代理或提醒用户）；模板 06 协作协议加「跨平台兜底：子代理上下文」强制条目（后台派发裸子代理入场先 read references 全文）；SKILL.md 强门禁补「覆盖范围与兜底」（inherit 路径靠门禁、裸子代理由 role 模板保底，不削弱硬规则）。references/场景/ 三文件交叉引用 19 处，模板 06↔SKILL↔adaptation 措辞一致，无断链。 |


|  |  |  |  |  |
