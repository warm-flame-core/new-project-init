# 全新项目问询 · 完整题库（reference 文件）

> 🗂️ **定位**：本文件是「全新项目」场景（11 轮）的**完整问询题库 + 细化执行流程**。SKILL.md「执行流程·全新项目」节只保留骨架与强制入口——**执行全新场景前必须读完本文件全文**，凭 SKILL.md 骨架直接问询 = 违规（见 SKILL.md「跨平台强门禁」）。
> 🔒 **加载保证**：Reasonix 运行时会自动折叠（auto-fold）；DSH 等不自动折叠的平台靠 SKILL.md「未读本文件全文=违规」硬门禁强制去读。**不要只读本文件开头就开问**。
> 📍 **变更记录方向**：本文件是 skill 内部参考资源，不是产出文档，无「变更记录」表要求；修改后同步 SKILL.md 版本表 + `docs/CREATION-LOG.md`。

---

## 问询节奏（轻重分级）

> **问询节奏**：🔴 = 关键决策，**一次一个**（每题附「推荐 + 权衡」展开讨论，答不上来给推荐默认并标注「可改」）；其余 🟡 = 批量（每题带推荐默认，整轮可一键「以上都用默认」快进）。分级标准：决策强度高 + 默认不可靠 → 🔴；有可靠默认 / 影响轻 → 🟡。🔴 题在批量轮之外独立问（可穿插在对应轮次开头）。

> **每轮末尾统一有「本组备注（填空）」**：补充这组问题没覆盖到的想法/约束？没有填「无」即可（必答但低打扰，不答不进入下一轮）。有实质内容的备注 → 记入 project-context「关键技术决策记录」或相应模板章节，避免聊完就丢。

---

## 第 1 轮 A：项目与技术栈

- 🔴 A0 产品形态？（决定后续所有技术栈走向，一次一个）① Web 网页 ② 小程序 ③ 桌面应用（含 C++ 桌面）④ 移动 App ⑤ 嵌入式/硬件（MCU/板卡/RTOS）⑥ 后端服务（无前端，API/固件配套）⑦ 多端（填空）
- 🔴 A2 技术栈主选？（各方向权衡：生态/团队熟悉度/交付目标，一次一个；选后 A2-2~A2-5 跟随批量）
  - A2-1 语言/框架系：① Java（Spring Boot+Maven/Gradle）② Python（FastAPI/Django+pip/uv）③ Node（Express/Nest+npm/pnpm）④ Go（Gin+go mod）⑤ **C++ 后端**（CMake/Makefile+g++/clang++，C++17/20，服务端应用）⑥ **嵌入式**（STM32/ESP32+PlatformIO/Keil/IAR，arm-none-eabi-gcc 交叉编译）⑦ C/C++ 混合 ⑧ 前端（Vue/React+Vite）⑨ 全栈组合 ⑩ 自定义
  - A2-1b（选⑥嵌入式时追问）：MCU/平台？① STM32 ② ESP32 ③ 其他（填空）；RTOS？① FreeRTOS ② 裸机 ③ 其他；烧录/调试？① OpenOCD/J-Link ② ST-Link ③ PlatformIO ④ 串口 ISP
  - A2-1c（选⑤C++ 后端时追问）：构建系统？① CMake ② Makefile ③ Meson；语言标准？① C++17 ② C++20；网络/服务框架？① 无（自研）② Boost.Asio ③ 其他
  - A2-2 构建/依赖工具？① Maven ② Gradle ③ pip/uv ④ npm/pnpm ⑤ CMake/Makefile ⑥ PlatformIO ⑦ 交叉编译链（arm-none-eabi-gcc）⑧ 其他
  - A2-3 数据库？① MySQL ② PostgreSQL ③ SQLite ④ 嵌入式/无库 ⑤ 暂无
  - A2-4 配置管理？① 配置文件（yml/.env）② 环境变量 ③ C 系头文件/宏 ④ 无
  - A2-5 部署/运行？① 本地 ② 服务器/Docker/systemd ③ 嵌入式烧录（JTAG/SWD/串口）④ 其他
- A3 新项目还是已有骨架？① 空目录 ② IDE 已生成骨架（算新项目不写代码）
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 2 轮 G：知识面与学习目标（决定 agent 后续怎么说话）

- G1 编程基础？① 新手 ② 学过语法 ③ 熟练一种语言 ④ 多语言熟练
- G2 本项目技术栈熟吗？① 不熟 ② 会一点 ③ 熟 ④ 就是为学它
- G3 工具链熟吗（git/命令行/IDE）？① 不熟 ② 基本会用 ③ 熟
- G4 参与目的？① 练手 AI ② 学特定技术 ③ 完成任务交付 ④ 职业方向探索
- G5 未来技术方向？① 当前栈深挖 ② 转其他（→ 填空，如嵌入式/C 系）③ 没定
- G6 术语解释偏好？① 通俗+解释 ② 正常术语 ③ 术语+原理深讲
- → G4=练手时，追问 P 组（练手模式）：P1 代码谁写？① 我手敲 agent 指导 ② agent 写我读懂 ③ agent 全包 ④ 混合；P2 审查谁读？① 我自己读报告+代码 ② agent 审查我复述 ③ 全交给 agent；P3 写代码要讲解为什么吗？① 详细（学习向）② 简要 ③ 不用；P4 完成标准？① 能跑通 ② 跑通+我理解 ③ 我能独立复现
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 3 轮 B：团队与工作流

- 🔴 B1 协作方式？（决定精简 or 完整模式 + agents 是否建，一次一个；解释差异后选）① 单人+agent（→ 可选精简模式，见下）② 多 agent 分工 ③ 手动为主
- 🔴 B2 项目性质？（与 B1 联动定模式，一次一个）① 个人练手/课程设计（→ 精简模式）② 个人认真项目 ③ 团队/多人（→ 完整）
- B3 模块拆分粒度？① ≤200 行/模块（推荐）② 放宽 ③ 无所谓
- B4 Git 分支策略？① main+feature ② main/develop/feature ③ 单分支
- B5 提交信息格式？① `[feat] module-XXX: 描述` ② 常规 ③ 无所谓
- B6 验收门槛？① 审查+测试报告+真实冒烟 ② 只要自测 ③ 简化
- B7 commit/push 谁执行？① agent 需用户明确允许（推荐）② 可自主
- B8 精简模式下：① 只要 README+CLAUDE.md+memory 三件套 ② 加 logs ③ 加测试手册；流程：① 单 agent 直做不拆角色 ② 仍要模块闭环但不要审查 ③ 照常
- B9 方案确认习惯？① 先讨论确认再动手（推荐）② 直接干，有问题再改 ③ 大改动先确认、小改动直接干
- B10 开发中你希望什么时机介入？① 每阶段结束看产出 ② 出问题才介入 ③ 全托管不打扰
- B11 进度汇报偏好？① 每步汇报（详细）② 阶段汇报 ③ 收尾汇总 ④ 简要（只报结论）
- B12 遇阻塞/歧义（环境问题/需求不清）？① 先问用户（推荐）② 先试默认方案，不行再问 ③ agent 自主决定
- B13 署名/签字规则？（四段式 `<实体人>-<平台>-<角色>@<分支>`，如 warmflame-Reasonix-Developer@feat/perm-008；无 git 省略 `@分支`；内部文档简式 `<平台>-<角色>`；单人可简化 `<角色>`）① 按四段式（推荐）② 自定义（填空）③ 不需要
- B14 平台/角色清单？（项目用哪些平台（Reasonix/DSH/Claude Code 等）、各角色叫什么名——`<实体人>` 缺省取上传 gitee/github 用户名）① 现在登记（列出：平台-角色）② 用默认（平台-角色）③ 不需要
- B15 哪些文档需要署名？① docs 变更记录 + specs 签署 + 验收结论（推荐）② 仅验收/签署处 ③ 全部文档
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 4 轮 C：记忆库边界

- 🔴 C1 **memory/ 是否入库？**（必问，影响 .gitignore + 记忆纪律；利弊讲清后选）① 仅本地不入库（.gitignore 加行）② 入库共享
- C2 记忆边界？① 记状态/决策/索引/活动/债务/阻塞，不记琐碎（推荐）② 全记 ③ 精简
- C3 logs 细档启用？① 按角色+日期四表（推荐）② 不启用 ③ 按模块
- C4 活动日志粒度？① 每模块每阶段 1 行（推荐）② 每动作
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 5 轮 D：编码规范

- D1 注释？① public 方法 Javadoc + 复杂逻辑行内注释 + 魔法数字常量（推荐）② 宽松 ③ 严格
- D2 统一返回格式？① `{code,message,data}` ② REST 原生 ③ 其他
- D3 错误码分段？① 按模块分段 ② 不细分
- D4 安全/日志？① 密码 BCrypt + 敏感信息禁打日志 ② 从简
- D5 日志级别约定？① ERROR/WARN/INFO/DEBUG 分层（推荐）② 简单 ③ 无所谓
- D6 代码行数上限？① 方法≤50/类≤500/模块≤200（推荐）② 宽松 ③ 无所谓
- D7 注释/文档语言？① 中文（推荐）② 英文
- D8 认证方式？① JWT ② Session ③ OAuth/第三方 ④ 暂不需要
- D9 角色清单？① 简单（管理员/普通用户）② 多角色（列出）③ 暂无
- D10 数据权限？① 按角色过滤（推荐）② 不控 ③ 复杂组织架构
- D11 版本号格式？① `X.Y.Z-module-XXX` ② 纯 semver ③ 无所谓
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 6 轮 git：上传规则（3 层）

- 第 1 层（不问，直接写 .gitignore）：编译产物/依赖目录/密钥环境/临时文件→不上；锁文件/编排文件/README→上
- 第 2 层（必问）：
  - 🔴 GIT-1 memory/、CLAUDE.md、内部手册？（敏感决策：入库 vs 本地，讲清利弊后选）① 本地保留不上（推荐）② 入库
  - GIT-2 IDE 配置（.idea/.vscode）？① 不上 ② 共享配置上
  - GIT-3 种子/测试数据？① 原样上 ② 脱敏后上 ③ 不上
  - GIT-4 大二进制（PDF/图片/jar）？① 上 ② Git LFS ③ 不上
- 第 3 层（兜底）：生成 .gitignore 后核对——clone 能否编译？该上没上（锁文件）？不该上的上了（密钥）？
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 7 轮 E：文档习惯

- E1 docs/ 要哪些？（多选）① 功能说明 ② 功能模块设计文档（权限/异常/性能/安全）③ 人话版说明（客户/答辩）④ 数据库设计 ⑤ 演进任务清单 ⑥ 测试手册 ⑦ 部署文档 ⑧ 分工文档（多人）⑨ 都要
- E2 文档给谁看？① 团队内部 ② 要交差（论文/客户）③ 两者
- E3 README 要吗？① 要（简介/快速开始/目录结构/账号）② 不要
- E4 人工接口文档？① 要（按模块端点表）② 只要自动生成 ③ 不要
- E5 结构设计文档？① 要（架构图/目录/分层/调用链路）② 不要
- E6 功能说明带非功能需求章节？① 要（性能/安全/合规）② 不要
- E7 配置示例模板？① 要（如 application.example.yml 留空密钥）② 不要
- E8 specs 五件套全量还是简化？① 全量五件套 ② 简化（plan+changelog）
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 8 轮 F：其他

- F1 团队/个人既有规范？开放式
- 🔴 F2 涉及敏感数据（身份证/手机号/隐私）？（有则触发脱敏/加密/日志红线规则，一次一个）① 有（→ 加脱敏/加密/日志红线规则）② 无 ③ 不确定（按有处理）
- F3 合规要求？① 无 ② 有（等保/GDPR/学校规定，描述）
- F4 有批量导入/导出需求？① 有（→ 建 file-templates/README）② 无
- F5 多仓库/微服务？① 单仓 ② 前后端分仓 ③ 多服务多仓（→ 每仓精简 CLAUDE.md+memory，总规范放主仓）
- F6 交付对象？① 自己/团队内部 ② 论文/答辩 ③ 客户/学校
- F7 交差要交付文档？① 演示脚本 ② 成果说明 ③ 正式测试报告 ④ 部署手册
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 9 轮 H：会话上下文管理

- H1 启用会话上下文管理纪律？① 启用（压缩 1 次提醒开新会话）② 不启用 ③ 更宽松（2 次）
- H2 提醒流程？① 标准（答「是」→先写交接文档再开新会话；答「否」→说明压缩坏处）② 简化
- H3 开新会话前交接什么？① 记忆库三件套 ② 交接文档 ③ 测试手册 ④ 全部（推荐）
- H4 交接文档放哪？① memory/handoff/（推荐——给 agent 看的放记忆库，**不放 docs/**）② memory/logs/ 交接记录表；**看完后处理方式**？① 完成即删 ② 改名（如「完成-日期-模块」）归档进 memory/handoff/archive/（方便回溯）③ 结合：完成即删、需留档的改名归档
- H4b 交接文档内容结构？① 按模板 25（**§1 文档签署状态清单必填**——版本/已签状态/还欠谁/接手动作，防换会话丢失 plan 审批/acceptance 签署状态，推荐）② 自由发挥
- H5 阶段边界提示？① 开发→审查→测试边界（推荐）② 只压缩提醒 ③ 每模块结束
- H6 用户答「否」不开新会话？① 说明坏处后尊重选择 ② 强制开
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 10 轮 T：开发工具构建

- 🔴 T1 开发中会有反复用的工具吗？（查表/建表/数据校验/清理测试数据——本项目曾反复临时构建校验码核实器/查表 Java/清理脚本，用完即弃浪费 token；决定 tools/ 是否建，一次一个；有则 T4 凭据上传紧随）① 有（→ 建 tools/ 目录）② 无
- T2 工具放哪？① 项目内独立 `tools/` 目录（推荐）② `scripts/` ③ 不独立
- T3 工具给谁用？① 只 agent 用（代码注释够用）② agent+人都用（要命令说明）
- T4 工具要上传吗？① **本地保留**（含凭据的敏感工具，如查表工具带连接串）② 入库（无敏感信息通用工具）
- T5 工具要文档吗？① 要 `tools/README.md`（模板 23：用途/用法/参数/安全说明/复用记录）② 只要注释
- T6 模块收尾工具与方法提炼检查？① 每模块收尾执行（推荐：模块五件套+验收完成后，回看本模块 logs/activity + 本次临时脚本 + 现有 tools/ 顺手度 + **logs 中反复出现的问题→解法模式**；产出分两类——**工具类**（可执行，复用 ≥2 场景 → 固化 tools/，删除演练）与**方法类**（固定解法/经验 → 项目内沉淀 CLAUDE.md 已知坑 / tools/README 使用注意；**判定跨项目通用 → 写全局记忆**）；**无产出也在 logs 记一行检查记录**）② 仅初始化收尾提醒一次 ③ 不启用
- T7 工具用什么语言/形式构建？① 与项目主语言一致（推荐，环境现成）② 可用其他语言（→ **先检测本机环境**：python/node/go 等是否可用，没有则提示用户安装或放弃该语言）③ 用户指定；**无论哪种，临时工具依旧遵守依赖方向硬规则 + 删除演练 + 含凭据本地保留**
- **依赖方向规则（硬规则，不劳用户选）**：
  - 规则 A（单向依赖）：`工具 → 项目` 允许（工具可复用项目依赖库/配置/工具类/entity）；**`项目 → 工具` 禁止**——项目代码严禁 import/引用 `tools/` 下任何内容
  - 规则 B（可删除性）：**任何工具——无论是否上传——被 delete 后，项目必须照常编译运行**
  - 落地验证（删除演练）：工具固化进 `tools/` 时执行一次——删掉该工具 → 跑 `mvn compile`（或前端 `npm run build`）→ 通过才允许固化；验证结论记入 tools/README.md 复用记录或 memory/logs
  - 联动 T4：含凭据工具强制本地保留 + .gitignore 排除，绝不上传
- 固化判定规则：**会在 2 个以上场景复用的工具 → 固化进 tools/**；一次性工具用完即弃，但用法记入 memory/logs 避免重造
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

## 第 11 轮 U：环境与数据治理

- U1 有真实环境/共享库吗？以谁为准？① 有共享库，schema.sql 为权威（推荐）② 有但脚本为准 ③ 无，纯本地
- U2 环境漂移了谁处理？① 写幂等迁移脚本修复（推荐）② 手动改库 ③ 记录待处理
- U3 有没有已知业务规则 vs DB 约束冲突？（本项目：user.username 全库唯一 vs 跨校同学号需求）① 有（→ 记录进 project-context 决策）② 无 ③ 不确定
- U4 多分支并行开发吗？（迁移号管理——本项目 V12 曾撞号改 V14）① 会（→ 约定迁移号分配/撞号重命名规则）② 单分支
- U5 敏感数值列格式防御？（学号/手机号/身份证被 Excel 存成数值型丢精度）① 模板文本格式+字符串解析（推荐）② 不管
- U6 工具链版本坑要记录吗？① 要（记录进部署文档"已知坑"+技术债务）② 不用
- U7 前后端错误码契约？① 401=未认证/403=无权限/登录失败非 401（推荐）② 简单
- U8 测试数据清理纪律？① 冒烟后恢复基线，DELETE 需确认（推荐）② 不用清理
- U9 模块编号管理？① 演进清单与流程编号对齐规则 ② 无所谓
- U10 开发机环境预检？① 要（初始化时实测 OS/工具链版本与可用性，结果+已知坑写入 CLAUDE.md 常用命令与部署文档「已知坑」）② 不用
- U11 有外部数据源（共享库/其他库/第三方 API）？agent 怎么访问？① 有，用现成工具/MCP ② 有，需临时脚本（→ 进 tools/ 固化）③ 无
- U12 数据库写操作（DDL 迁移/数据修改）谁确认？① 用户确认后执行（推荐：先展示 SQL 获确认再执行）② agent 自主但记录 ③ 纯只读开发
- U13 要配 CI/CD 吗？① 暂不（推荐先跑通再配）② 要（→ 部署文档补 CI 章节）③ 已有（沿用）
- U14 需要审计日志（操作留痕/合规）？① 需要（→ 数据表+权限设计纳入）② 不需要
- U15 开发机常用方法与固定解法？① 有（→ 初始化时沉淀「现象/根因/解法」到 CLAUDE.md 已知坑 + tools/README 使用注意；**跨项目通用的（如 Windows 中文乱码/编码）→ 记入全局记忆**，下次任何项目初始化直接预置）② 无 ③ 不确定（按有处理，遇到再补）
- U 组答案映射（不新增模板）：U1/U2→02+15；U3→02 决策；U4→01 版本管理；U5→22 格式约定；U6→15 已知坑；U7→01 接口规范；U8→14 通用准备+05 CLEAN；U9→13 变更记录+02 注意事项；U10→01 常用命令+15 已知坑；U11→23 工具列表+02 外部依赖；U12→01 记忆纪律+23 安全说明；U13→15 部署；U14→01 接口规范+09 非功能；U15→01 已知坑+23 使用注意+全局记忆
- **本组备注（填空）**：没覆盖到的想法/约束？没有填「无」。

---

## 装配问询与平台主导（公共，跨场景，v11.0）

> 🔧 问完全部轮次后，按下述补问一组「规范装配」题（规则详见 SKILL.md「规范装配问询」节）：
> - **每套可装配规范**（中文排版 27 / commit 与 git 28 / PR 29 / git worktrees BL-01 / 并行 agent BL-02 / 中文审查输出 BL-05）逐条问「要不要 + 你有没有自有规范或想法」；答「要」后按**内容量裁决**——内容多→独立文件，仅小建议→并入 `<入口规范文件>` 对应节。有自定义 commit 规范 → 结合用户规则优化，并在特化文件开始前**告知用户优化结果**。
> - **平台主导问询（ISSUE-013）**：项目用哪些平台？主导平台（写正文的那份）是哪个？→ 决定入口规范文件名 + 非主导平台薄入口（见 SKILL.md「平台入口规范文件」节，缺省 agent 推断）。

> 问完全部轮次后 → 回到 SKILL.md「执行流程·探索与产出」按结论特化文件。
